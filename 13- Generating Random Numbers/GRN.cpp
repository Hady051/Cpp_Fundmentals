# include <iostream>
# include <cstdlib>
# include <ctime>

using namespace std;

int main()
{
    /*
    long elapsedSeconds =  time(nullptr);  // returns number of seconds from 1/jan/1970
    srand(elapsedSeconds);
    int rand_number = rand() % 6; // this function needs a seed value to give different random numbers
    if (rand_number == 0) {
        rand_number += 1;
    }

    //cout << elapsedSeconds;
    cout << rand_number;

    return 0;
    */


}
